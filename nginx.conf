sudo cat /etc/nginx/sites-available/backstage
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name 34.180.12.62; # <--- YOUR PUBLIC IP HERE

    # SSL Configuration (already done)
    ssl_certificate /etc/nginx/ssl/backstage.crt;
    ssl_certificate_key /etc/nginx/ssl/backstage.key;
    # ... other SSL settings ...

    # Proxy for API backend (still HTTP internally)
    location /api/ {
        proxy_pass http://localhost:7007/api/; # Backend is HTTP
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Server $host;
    }

    # Proxy for frontend (NOW HTTPS INTERNALLY)
    location / {
        proxy_pass https://localhost:3000/; # <--- CHANGE THIS TO HTTPS
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Server $host;
    }
}
